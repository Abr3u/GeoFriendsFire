USA
latitude 25 50
longitude -125 -70
=
3670300

New York
inferior left -> 40.543155, -74.056834
superior right -> 40.904894, -73.726044
=
130425

Kmeans clusters
k=2 -> 3.2098474496243083E-9
k=4 -> 4.671376571847712E-10
k=5 -> 2.701799675821211E-10
k=6 -> 2.356666682945554E-10
k=8 -> 2.1862730014317916E-10

__________________________________________


But you can write a generic class ValueHolder<T> and mark the variable of it final! 
And then just instead of "a = val" you write "aHolder.setValue(val)"! It's so easy!

Objects.hashCode(mId, mTitle, mDescription);
Task task = (Task) o;
        return Objects.equal(mId, task.mId) &&
               Objects.equal(mTitle, task.mTitle) &&
               Objects.equal(mDescription, task.mDescription);



Uri gmmIntentUri = Uri.parse("geo:"+country.lat+","+country.lng+"?q="+country.name+"&z=2");
        navigator.startActivity(Intent.ACTION_VIEW, gmmIntentUri);
		
protected final FragmentActivity activity;
activity.startActivity(new Intent(action, uri));
__________________________________________

Today:
eventsNearby e MyEvents done!
problema do workload done! - resolvido incrementando o raio ate ter o numero de eventos - pode exceder o workload pela distancia minima (default)
login done!
drawerView done! - resolvido com dynamicPagerAdapter e fragmentKeys
eventDetails done!
settings done!
direction done! - resolvido com longClick num evento (cardview)


Tomorrow:
por alert dialog antes de ir para directions
testar workload/furthest!!

ver exemplos de rxJava de onlineDetection;
ver exemplos de rxJava de polling

comecar com amigos??
track de localizacoes - maybe RxLocation?
clustering de markers, mapa style

Extra:
criar anotation para ver se tem wifi/gps
mvvm
mostrar ao user os seus clusters?
api google maps, directions, places, roads;
